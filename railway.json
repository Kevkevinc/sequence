{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'worker service: no build step, runs via tsx'"
  },
  "deploy": {
    "//": "MALLOC_ARENA_MAX=2 stops glibc hoarding freed memory across many arenas, which keeps the Node worker's resident size down. That headroom matters most during a 4K render, where ffmpeg needs ~1.1GB in the same container.",
    "startCommand": "MALLOC_ARENA_MAX=2 npm run worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
